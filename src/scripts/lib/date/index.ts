import { LibDateInterface } from "../interfaces"
import {
  YEAR,
  DATE_FORMAT_DD_MMM_YYYY,
  DATE_FORMAT_YYYY,
  now,
} from "./variables"

const LibDate: LibDateInterface = {
  YEAR,
  DATE_FORMAT_DD_MMM_YYYY,
  DATE_FORMAT_YYYY,
  now,
}

export default LibDate
